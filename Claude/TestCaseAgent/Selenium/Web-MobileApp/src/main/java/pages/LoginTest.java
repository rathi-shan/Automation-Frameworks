/*
package pages;


import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class LoginTest {
    WebDriver driver;
    By user_name= By.id("login");
    By user_pwd= By.id("password");
    By login_button = By.id("login_button");

    public LoginTest(WebDriver driver){
        this.driver=driver;
    }


    public void SignIn(String usr, String pwd){

       driver.findElement(user_name).sendKeys(usr);
       driver.findElement(user_pwd).sendKeys(pwd);
       driver.findElement(login_button).click();

    }
}
*/